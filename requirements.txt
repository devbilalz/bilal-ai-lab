# ============================================================
# Bilal AI Lab — Dependency Manifest (requirements.txt)
# ------------------------------------------------------------
# This is a Node.js / Next.js project. The AUTHORITATIVE manifest
# is package.json (+ pnpm-lock.yaml). This file is a human-readable
# mirror of every dependency and why it exists.
#
# Install everything with:   pnpm install
# (Node 20.19.2, pnpm 9.15.9 via Corepack)
# ============================================================

# ---- Runtime framework ----
next==16.2.10                 # App Router, SSR/SSG, routing, metadata/SEO
react==19.2.4                 # UI runtime
react-dom==19.2.4             # DOM renderer

# ---- Styling / UI utilities ----
tailwindcss==4.3.2            # utility CSS (v4, engaged via globals.css)
@tailwindcss/postcss==4.3.2   # Tailwind v4 PostCSS pipeline
clsx==2.1.1                   # className builder (lib/utils cn())
tailwind-merge==3.6.0         # merge/de-dupe Tailwind classes (cn())
lucide-react==1.24.0          # icons (nav menu) — note: no brand icons in 1.x

# ---- Animation / interaction (ENGAGED) ----
motion==12.42.2               # Framer Motion — Reveal, CountUp, TerminalText,
                              #   boot/wow overlay, timeline scroll, panels
lenis==1.3.25                 # smooth scroll provider (reduced-motion aware)

# ---- Graph / visualization (ENGAGED) ----
@xyflow/react==12.11.2        # React Flow — S2 Mission Control pipeline graph

# ---- Theming ----
next-themes==0.4.6            # dark-first theme provider

# ---- Analytics ----
@vercel/analytics==2.0.1      # privacy-friendly page analytics

# ---- Fonts ----
# NOTE: fonts are loaded via next/font/google (Geist, Geist_Mono) in app/layout.tsx.
geist==1.7.2                  # self-hosted Geist package (installed w/ scaffold;
                              #   currently redundant — next/font/google is used)

# ---- Animation / choreography (INSTALLED, RESERVED for Phase 3) ----
gsap==3.15.0                  # reserved for S9.5 architecture-diagram reveals
@gsap/react==2.1.2            # useGSAP hook (reserved, not yet imported)

# ============================================================
# devDependencies (tooling — installed via pnpm install)
# ============================================================
typescript==5.9.3             # strict TS
eslint==9.39.5                # linting
eslint-config-next==16.2.10   # Next.js lint rules
@types/node==20.19.43
@types/react==19.2.17
@types/react-dom==19.2.3

# ============================================================
# NOT YET INSTALLED — future phases (see Development Plan §5)
# Add only when the feature that needs them is built:
#   @next/mdx @mdx-js/loader @mdx-js/react @types/mdx   # S9/S10 MDX content
#   shiki rehype-pretty-code                            # code snippets (if real)
#   three @react-three/fiber @react-three/drei          # S1.2 optional 3D (deferred)
# ============================================================
